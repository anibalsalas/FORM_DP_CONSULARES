import { NoNegativosDirective } from './no-negativos.directive';

describe('NoNegativosDirective', () => {
  it('should create an instance', () => {
    const directive = new NoNegativosDirective();
    expect(directive).toBeTruthy();
  });
});
